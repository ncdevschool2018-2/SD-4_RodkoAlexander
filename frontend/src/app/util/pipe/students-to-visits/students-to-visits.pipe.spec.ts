import { StudentsToVisitsPipe } from './students-to-visits.pipe';

describe('StudentsToVisitsPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentsToVisitsPipe();
    expect(pipe).toBeTruthy();
  });
});
