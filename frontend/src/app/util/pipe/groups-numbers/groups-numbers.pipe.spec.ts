import { GroupsNumbersPipe } from './groups-numbers.pipe';

describe('GroupsNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupsNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
